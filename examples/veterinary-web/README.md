# VetCare — Veterinary Clinic Landing Page

Modern landing page built with **React + Tailwind CSS** + Vite.

## Built with qwen3.6:35b-a3b

This project was generated by **[qwen3.6:35b-a3b](https://ollama.com/library/qwen3.6)**, a 35-billion-parameter open-weight language model from the Qwen family, running locally on a **NVIDIA Tesla P40** (23 GB VRAM) with a **64K context window**.

### Why this model?

qwen3.6:35b-a3b's strong reasoning, code generation, and instruction-following capabilities make it ideal for local AI tasks — generating production-ready React components, Tailwind layouts, and full project scaffolds entirely offline with zero API calls needed.

### Hardware context

- GPU: NVIDIA Tesla P40 (23040 MiB VRAM)
- Context: 64K tokens

## Tech Stack

- [Vite](https://vite.dev/) — bundler
- [React 19](https://react.dev/) — UI
- [Tailwind CSS](https://tailwindcss.com/) — styling
- [Lucide React](https://lucide.dev/) — icons
- Images from [Unsplash](https://unsplash.com) (direct CDN links)

## Quick start

```bash
# Install dependencies
pnpm install

# Dev server
pnpm dev        # → http://localhost:5173
pnpm build      # → dist/
pnpm preview    # production preview
```

## Project structure

```
src/
├── components/
│   ├── Hero.jsx           — hero section with nav, stats & CTAs
│   ├── Services.jsx       — 6-service card grid
│   ├── Team.jsx           — medical team profiles
│   ├── Testimonials.jsx   — client reviews with star ratings
│   ├── Contact.jsx        — contact info + appointment form
│   └── Footer.jsx         — footer with social links
├── assets/                — image references (Unsplash URLs)
├── App.jsx                — root layout
├── main.jsx               — entry point
└── index.css              — Tailwind directives
```

## Sections

| Section | Content |
|---------|---------|
| Hero | Background image, key stats, dual CTAs |
| Services | 6 cards with icons (general, vaccination, surgery, lab, dental, emergencies) |
| Team | 3 doctor profiles with photos and specialties |
| Testimonials | 3 reviews with star ratings on dark background |
| Contact | Info cards + appointment booking form |
| Footer | Logo, copyright, social media links |

## Design

- Palette: emerald / teal / cyan (veterinary green)
- Typography: Inter (Google Fonts)
- Responsive: mobile-first with `sm`, `md`, `lg` breakpoints
- Smooth animations via Tailwind transitions + hover effects
- Sticky navbar with blur backdrop + hamburger menu on mobile
